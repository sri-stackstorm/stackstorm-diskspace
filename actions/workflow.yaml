# This is a sample workflow metadata file generated by Stackstorm VSCode Extension, Change as required.
---
description: A sample workflow for cleaning up /var diskspace
enabled: true
runner_type: orquesta
entry_point: workflows/diskspace.yaml
name: var-diskspacecleanup
pack: diskspace
parameters:
  hostname:
    type: string 
    required: true 
  filetype:
    type: string 
    required: true 
  # cmd:
  #   required: true
  #   type: string
  # timeout:
  #   type: integer
  #   default: 60
